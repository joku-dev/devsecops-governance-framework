# L1 Baseline Release Package v1.1.2

## Purpose

This package is the final patch release of the versioned DevSecOps `L1` baseline for downstream repositories.

Its purpose is to correct the full reusable-workflow SHA used by the released wrapper so the governance run input contract works in real consumer repositories.

## What Is New In v1.1.2

- corrected full reusable workflow SHA in the released wrapper
- corrected downstream workflow example to use `l1-baseline-v1.1.2`
- preserved the governance run input contract support introduced in `v1.1.0`
