# GCR-2026-079: Consumer revalidation and first concrete lifecycle case

## Request and artifact classification

The maintainer authorized revalidating the three existing consumers, then
preparing a bounded lifecycle extension for a real consumer finding.

| Field | Classification |
|---|---|
| Artifact types | Downstream evidence snapshots, generated indexes/viewer, diagnostic candidate bundle, operating decision brief and reference run |
| Target paths | `status/`, `generated/reports/lifecycle-candidates/`, generated projections, `docs/operations/` |
| Owner | Repository maintainer; consumer lifecycle decision/closure authority remains to be appointed |
| Source Document Intake | No; actual runtime evidence and explanatory operating preparation |
| Evidence contract impact | None; existing intake and diagnostic candidate contracts |
| Runtime impact | Consumer CI-local diagnostics and open action in demo-consumer PR #5; no central lifecycle acceptance or new blocking |
| Release impact | None; released baselines and accepted LD-07 implementation remain unchanged |
| Validation | Pinned complete suite, strict docs, demo smoke, candidate reproduction and source binding, latest-result selection, accepted implementation fingerprint |

## Scope and boundary

Refresh existing workflows manually on their real main commits and retain the
correct diagnostic context. Use the existing intake scripts locally and publish
one coherent central PR; do not fabricate central workflow telemetry for local
collection. The demo-consumer and Factory DevSecOps producers use the released
single-gate fallback artifact, not full control evaluations.

Select only the demo consumer's `operation_readiness` gate, with two explicit
source messages. Prepare real function diagnostics and a linked open feedback
action in the consumer; preserve `reviewed` declarations until accountable
evidence approval. The four diagnostic gate candidates remain outside accepted
lifecycle state. Do not invent marker findings or infer closure from CI success.

The existing LD-07 request explicitly excludes other consumers. A new scope,
role assignment, evidence sufficiency decision, versioned admission and later
implementation-bound operating acceptance are required for live consumer operation.
The [concrete brief](../../operations/evidence/consumer-lifecycle-operation-readiness.md)
and [reference record](../../operations/reference-runs/2026-09-13-consumer-revalidation.md)
record exactly what was prepared and what remains personal.

Use the maintainer's standing technical PR review exception; preserve all checks
and restore the normal review requirement immediately after merge.
