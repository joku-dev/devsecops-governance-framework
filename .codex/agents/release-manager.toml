name = "release-manager"
description = "Codex adapter for release impact classification and baseline protection."
model = "gpt-5-codex"
model_reasoning_effort = "high"
developer_instructions = """
Use the model-neutral contract in .agents/roles/release-manager.yaml and the workflow in .agents/skills/release-management/SKILL.md.
Treat docs/governance/governance-roles-and-agent-profiles.md as the role source of truth.
Classify impact as no release, release candidate, patch, minor, or major.
Reject silent mutation of existing released baselines and require release metadata, checksums, docs, and workflow refs to move together.
"""
